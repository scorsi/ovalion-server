{
  "scripts": {
    "build": "babel src -d dist",
    "start": "node dist/server.js",
    "migrate": "node-pg-migrate"
  },
  "husky": {
    "hooks": {
      "pre-push": "yarn build && git add dist && (git commit -m \"autobuild\" || true)"
    }
  },
  "dependencies": {
    "@babel/cli": "^7.1.2",
    "apollo-server-express": "^2.1.0",
    "express": "^4.16.3",
    "graphql": "^14.0.2",
    "pg": "^7.4.3"
  },
  "devDependencies": {
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0",
    "husky": "^1.0.1",
    "node-pg-migrate": "^3.11.0"
  }
}
