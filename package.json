{
  "scripts": {
    "build": "babel src -d dist",
    "run": "node dist/server.js",
    "start": "yarn build && yarn start"
  },
  "dependencies": {
    "@babel/cli": "^7.1.2",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0"
  }
}
